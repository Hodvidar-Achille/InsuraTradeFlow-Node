Command to start the App now it uses Docker:
docker-compose up --build

Command to access the DB in docker from the cmd on the host machine:
docker exec -i -t [db-image-name] mysql -uroot -p
docker exec -i -t insuratradeflow-mysql-v1 mysql -uroot -p
And then enter the DB root password

Command to show tables in the DB:
SHOW DATABASES;
USE InsuraTradeFlow;
SHOW TABLES;
SHOW COLUMNS FROM insurance_policies;
SELECT * FROM insurance_policies;

SHOW COLUMNS FROM deleted_policies;
SELECT * FROM deleted_policies;

Frontend App:
http://localhost:3000/
http://localhost:8080/swagger-ui/index.html#/

(!) MUST BE DONE when starting for first time go-to http://localhost:8024/
and perform initialization of the Axon server (Otherwise the deletion have issue) (TODO fix this)
Axon Dashboard:
http://localhost:8024/eventstore/Search
http://localhost:8024/swagger-ui/index.html#/

For rebuilding frontend
cd frontend
npm install
npm run build